import { setAppConfig } from '@helper/api';
import { setGaignsight } from '@helper/gainsight';
import { ApprovalsConfigurationPanel } from '@pages/AdminFlow';
import { ApprovalSelectionConfiguration } from '@pages/ExtensionFlow';

export { ApprovalsConfigurationPanel, ApprovalSelectionConfiguration, setAppConfig, setGaignsight };

// For standalone dev setup

// import React from 'react';
// import ReactDOM from 'react-dom/client';

// import App from './App';

// const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
// root.render(
//     <React.StrictMode>
//         <App />
//     </React.StrictMode>
// );
